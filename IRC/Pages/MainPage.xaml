<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="IRC.MainPage">
    <ContentView>
            <!-- 
        <StackLayout>
            <Button Clicked="Freenode_Button_Clicked"
                    Text="Connect Freenode"
                    WidthRequest="200"
                    HeightRequest="50"/>
            <Button Clicked="Libera_Button_Clicked"
                    Text="Connect Libera"
                    WidthRequest="200"
                    HeightRequest="50"/>            
            
            
        </StackLayout>
            -->
        <VerticalStackLayout Padding="20">
            <Label Text="Nick" />
            <Entry x:Name="NicknameEntry" />

            <Label Text="Username" />
            <Entry x:Name="UsernameEntry"/>

            <Label Text="Real Name" />
            <Entry x:Name="RealnameEntry"/>

            <Label Text="Hostname" />
            <Entry x:Name="HostnameEntry"/>

            <Label Text="Port" />
            <Entry x:Name="PortEntry" Keyboard="Numeric" />

            <Label Text="Password (Optional)" />
            <Entry x:Name="PasswordEntry" Placeholder="Enter password (Optional)" />

            <Button Text="Submit" 
                    HorizontalOptions="Center"
                    Clicked="OnSubmitClicked"/>
        </VerticalStackLayout>


    </ContentView>
</ContentPage>
